server:
  port: ${APP_SERVER_PORT:8082}

spring:
  application:
    name: project-api-server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${APP_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI:http://localhost:8080}
